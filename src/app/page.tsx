"use server";

import Hero from "@/components/hero";
import Blocks from "@/components/Blocks";

const Home = () => {
  return (
    <main>
      <Hero />
      <Blocks />
    </main>
  );
};

export default Home;
